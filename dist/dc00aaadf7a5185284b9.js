import*as THREE from"three";import"./style.css";const scene=new THREE.Scene,geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:16776960}),mesh=new THREE.Mesh(geometry,material);scene.add(mesh);const screen={width:900,height:700},camera=new THREE.PerspectiveCamera(75,screen.width/screen.height);camera.position.set(0,1,3),scene.add(camera);const canvas=document.querySelector(".webgl"),renderer=new THREE.WebGLRenderer({canvas});renderer.setSize(screen.width,screen.height),renderer.render(scene,camera);